% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_stpetersburg.R
\name{simulate_stpetersburg_experiment}
\alias{simulate_stpetersburg_experiment}
\title{Simulate multiple runs of the St Petersburg game}
\usage{
simulate_stpetersburg_experiment(
  n,
  B,
  c = 10,
  trim_frac = 0.1,
  mom_blocks = 10,
  seed = NULL
)
}
\arguments{
\item{n}{Integer; sample size (number of independent plays per run).}

\item{B}{Integer; number of independent simulation runs.}

\item{c}{Numeric; threshold factor for defining \eqn{N_n(c)}, the rare-event count
(default 10).}

\item{trim_frac}{Numeric in [0, 0.5); fraction for symmetric trimming in the
trimmed mean (default 0.1, i.e. 10\\% trimming).}

\item{mom_blocks}{Integer; number of blocks for the median-of-means estimator
(default 10).}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL}, the
current RNG state is used.}
}
\value{
A data frame with \code{B} rows, each corresponding to one simulation run,
with columns:
\describe{
\item{\code{n}}{Sample size for that run (equals the input \code{n}).}
\item{\code{S_n}}{Sum of the \code{n} payoff values in that run.}
\item{\code{mean}}{Ordinary sample mean, \eqn{S_n / n}.}
\item{\code{A_n}}{Scaled mean, \eqn{S_n / (n \log_2 n)}.}
\item{\code{N_n}}{Count of observations \eqn{\ge c \cdot i \log_2 i} in that run.}
\item{\code{M_n}}{Maximum payoff in that run.}
\item{\code{trimmed}}{Trimmed mean of the \code{n} observations (using \code{trim_frac}).}
\item{\code{median_of_means}}{Median-of-means estimate (using \code{mom_blocks} blocks).}
}
}
\description{
Runs the St Petersburg game experiment \code{B} times (each with sample size \code{n}),
recording sums, scaled means, extreme-event counts, maxima, and robust estimators
for each run.
}
\examples{
# Simulate 100 runs of sample size 1000
sim <- simulate_stpetersburg_experiment(n = 1000, B = 100, seed = 42)
head(sim)

}
